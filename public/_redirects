# Netlify redirects file for SPA routing
# This tells Netlify to serve index.html for all routes that don't correspond to actual files
# This allows React Router to handle client-side routing properly

# Serve index.html for all non-file requests (SPA fallback)
/*    /index.html   200
